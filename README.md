# Enhance for Machine

Project repository for machine vision enhancement and ROI coding workflows.

## Datasets
Dataset downloads and processed datasets are stored in `datasets/` (excluded from git via `.gitignore`).
